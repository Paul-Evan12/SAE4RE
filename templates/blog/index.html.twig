{% extends 'base.html.twig' %}

{% block title %}Bienvenue sur mon Portfolio{% endblock %}

{% block body %}
    <nav>
        <ul class="navbar">
            <li><a href="{{ path('app_blog_presentation') }}">Présentation</a></li>
            <li><a href="{{ path('app_blog_competences') }}">Compétences</a></li>
        </ul>
    </nav>

    <div class="container">
        <h1>Bienvenue sur mon Portfolio !</h1>
        <p>Je suis <strong>Paul-Evan</strong>, un développeur web passionné par la création de sites et applications innovants. Découvrez mes projets et mon parcours professionnel ci-dessous.</p>
        <a href="{{ path('app_blog_presentation') }}" class="btn">Voir ma présentation</a>
    </div>

    <footer style="position:fixed;bottom:0;left:0;width:100%;background:#333;color:#fff;padding:10px 0;z-index:999;text-align:center;">
        <p>&copy; 2025 {{ 'site.owner'|trans({'%name%': '[Ton Nom]'}) }}. Tous droits réservés.</p>
    </footer>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #222;
            color: #fff;
        }

        /* Barre de navigation en haut */
        nav {
            background-color: #000;
            padding: 12px 0;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1000;
        }

        .navbar {
            list-style: none;
            display: flex;
            justify-content: center;
            margin: 0;
            padding: 0;
            gap: 30px;
        }

        .navbar li {
            margin: 0;
        }

        .navbar a {
            color: white;
            text-decoration: none;
            font-size: 18px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .navbar a:hover {
            text-decoration: underline;
        }

        /* Espace pour que le contenu ne soit pas caché par la barre fixe */
        .container {
            background-color: #333;
            color: #fff;
            padding: 30px;
            border-radius: 10px;
            width: 80%;
            max-width: 600px;
            margin: 100px auto 20px; /* margin-top augmenté pour tenir compte de la nav fixe */
            text-align: center;
        }

        h1 {
            font-size: 36px;
            color: #fff;
            margin-bottom: 20px;
        }

        p {
            font-size: 18px;
            line-height: 1.6;
            margin-bottom: 30px;
            color: #fff;
        }

        .btn {
            padding: 12px 30px;
            background-color: #444;
            color: white;
            text-decoration: none;
            font-size: 18px;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        .btn:hover {
            background-color: #bbb;
            color: #222;
        }

        footer {
            background-color: #333;
            color: #fff;
            text-align: center;
            margin-top: 40px;
            font-size: 14px;
        }

        h1, h2, h3, h4, h5, h6, p, a, footer {
            color: #fff;
        }
    </style>
{% endblock %}